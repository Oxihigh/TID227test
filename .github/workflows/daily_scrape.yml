# name: Daily Toastmasters Data Scraper

# on:
#   schedule:
#     # Runs every day at 3:00 PM IST (9:30 AM UTC)
#     - cron: '30 9 * * *'
#   # Allows manual triggering from GitHub Actions UI
#   workflow_dispatch:

# jobs:
#   scrape-and-update:
#     runs-on: ubuntu-latest
#     permissions:
#       contents: write

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.10'

#       - name: Install dependencies
#         run: |
#           python -m pip install --upgrade pip
#           pip install -r requirements.txt

#       - name: Install Playwright Browsers
#         run: playwright install chromium

#       - name: Run scraper & mastersheet generator
#         run: python download.py

#       - name: Commit and push updated Mastersheet
#         run: |
#           git config --global user.name "github-actions[bot]"
#           git config --global user.email "github-actions[bot]@users.noreply.github.com"
#           git add -A
#           git diff --quiet && git diff --staged --quiet || git commit -m "Automated daily update of District 227 Mastersheet"
#           git push
